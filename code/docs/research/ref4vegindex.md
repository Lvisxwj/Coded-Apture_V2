研究是严谨的，不是讨好的。是就是是，待验证就是待验证，不合适就是不合适！我提出的所有猜想，都需要你严格的判别，需要认真的分析适不适合当前任务，然后再做决定，因此再所有的想法之后都需要置信度的说明


# 我觉得可能对vegindex有启发性的论文

## HyPyraMamba: A Pyramid Spectral Attention and Mamba-Based Architecture for Robust Hyperspectral Image Classification

code：https://github.com/dekai-li/HyPyraMamba
本地：G:\MachineLearning\vegetation index\reference\code\HyPyraMamba-master

整个模型就像一套精密的流水线，从补丁嵌入开始，到PSA、双路径Mamba、AEDC，最后到分类头，每一步都精准解决一个问题
高光谱的波段里，很多都是冗余信息，白占算力还干扰分类。这篇论文设计了PSA模块去分类。它先做多尺度下采样，用平均池化把相邻波段的冗余信息抹平，还不破坏光谱的连续性。然后给每个尺度做多头空间注意力，聚焦那些信息量大的波长。最后把多尺度特征拼起来，再投影回原维度——既保留了关键光谱信息，又把没用的冗余全筛掉了！
# 这个降维降参数量似乎非常有必要，ssr好像也用了主成分分析来降维 投影

普通Mamba搞不定3D高光谱，这篇直接拆成两条路：光谱Mamba：把光谱通道分成连续的组，每个组当成一个序列来建模，既保留局部光谱连续性，又能抓长程的光谱依赖；空间Mamba：把每个空间像素当成独立序列，专门强化空间结构的建模。最后再用注意力加权融合这两路特征，空间和光谱的信息彻底拧成一股绳，再也不会顾此失彼

光有Mamba还不够，论文还加了个AEDC模块专门补全多尺度空谱特征的细节：
用多个不同的卷积核（专家），每个核学不同的空谱特征，再用可学习的权重给这些专家的输出加权。哪怕是光谱长得几乎一样的地物，也能靠这些细微的特征差异精准区分！

整套架构不是简单拼模块，而是从输入到输出全链路优化：补丁嵌入先用1×1卷积降维，降低计算成本；每个模块后都加残差连接、组归一化，保证训练稳定；最后还做了计算复杂度分析，参数量只有121万，FLOPs也控制在合理范围，不是为了精度牺牲效率的“重模型”。

作者还做了超多细节实验，给实际应用划了重点：PCA维度选30最佳：既能去冗余，又能保留关键特征，维度太高反而加噪声；学习率0.0003最稳：四个数据集都能达到高精度，波动最小；训练样本不用多：每个类别30个训练样本就够，泛化能力拉满！
# 所以我们要做PCA的话要对自己数据的维度数量就不能照搬，需要预处理一下选出最好的维度能在保存信息和参数量之间找到平衡，而不是盲目的自己设定一个值

### MODA:TheFirst Challenging Benchmark for Multispectral Object Detection in Aerial Images
https://www.arxiv.org/pdf/2512.09489

#### 跨谱注意力特征细化模块（Cross-spectral Attention Feature Refinement，CAFR）
问题：①跨层特征存在光谱偏差问题：高层特征语义强，但空间细节弱；低层特征空间细节丰富，但语义弱。不同层的光谱响应分布存在不一致，即“光谱偏差”。这会导致跨层融合时信息不对齐、特征被破坏、最终影响目标判别能力。②多尺度特征融合缺乏“目标一致性”问题：特征融合通常是直接加和或拼接，缺少“目标导向”的约束，导致背景信息被放大，小目标特征被淹没，目标信息在不同层传播过程中逐渐衰减。

实现方式：CAFR（跨光谱注意力特征精炼模块）：采用自底向上的特征传播路径，利用目标感知特征进行跨层光谱注意力计算，缓解光谱偏差（spectral bias）问题，实现多尺度目标特征的进一步对齐与增强。通过构建跨层光谱注意力，将低层目标细节与高层语义信息进行自适应融合，从而实现目标导向的特征精细化表达。

#### 光谱引导自适应跨层融合模块（Spectral-guided Adaptive Cross-layer Fusion，SACF）
问题：①光谱信息在跨层融合中被破坏问题：传统特征金字塔在跨层融合时，忽略或弱化光谱信息的结构关系，导致同一目标内部的光谱一致性被破坏。②下采样导致的空间纹理细节丢失与谱混叠问题：在目标边缘和混合像素区域，特征提取过程中的连续下采样会造成空间纹理细节的严重丢失。

实现方式：实现“跨层特征重建机制”，通过一致性驱动的特征聚合、细节增强与自适应融合，实现语义信息与空间细节的协同优化，具备良好的跨任务迁移能力。



# 总结
阅读完整的论文，1选取可能对我们任务有用的架构，模型创新点等.2对这些有用的架构和创新进行二次创新，运行增 删 改一定的内容，使得更加符合我们植被指数的任务
